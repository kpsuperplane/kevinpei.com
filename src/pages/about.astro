---
import AppShell from '../layouts/AppShell.astro';
import PostView from '../components/PostView.astro';
import AboutContent, { frontmatter } from '../content/about.mdx';
import aboutRaw from '../content/about.mdx?raw';
import { getReadTime } from '../utils/readTime';

const readTime = getReadTime(aboutRaw.replace(/^---[\s\S]*?---\n?/, ''));
---

<AppShell
  title="About — Kevin Pei"
  description={frontmatter.description}
  playing={true}
  nowPlayingTitle={frontmatter.title}
  nowPlayingTags={frontmatter.tags}
  readTime={readTime}
>
  <PostView
    Content={AboutContent}
    title={frontmatter.title}
    description={frontmatter.description}
    tags={frontmatter.tags}
    linkTags={false}
    readTime={readTime}
  />
</AppShell>
